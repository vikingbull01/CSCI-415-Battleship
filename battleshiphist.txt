--Player 1 info
CREATE TABLE Player1Hist
(
	Player1Ships VARCHAR(4),
	Player1Fire VARCHAR(4),
	Player1Hits VARCHAR(4),
	Player1Misses VARCHAR(4)
);

--Player 2 info
CREATE TABLE Player2Hist
(	
	Player2Ships VARCHAR(4),
	Player2Fire VARCHAR(4),
	Player2Hits VARCHAR(4),
	Player2Misses VARCHAR(4)
);

--Hits table
CREATE TABLE HitsAndMisses
(	
	P1Hits VARCHAR(4),
	P2Hits VARCHAR(4),
	P1Misses VARCHAR(4),
	P2Misses VARCHAR(4)
);

--Colors table to identify actions/squares
--Black for ship
--gray for placed ship??
--red for hit
--pink for slected area to hit
--blue for water/miss
--white for blank area or nothing in it
CREATE TABLE Color
(
black VARCHAR(4),
gray VARCHAR(4),
red VARCHAR(4),
pink VARCHAR(4),
blue VARCHAR(4),
white VARCHAR(4)
);

