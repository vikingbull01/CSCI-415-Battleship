--Colors table to identify actions/squares
--Black for ship
--red for hit
--pink for selected area to hit
--blue for water/miss
--white for blank area or nothing in it yet

CREATE TABLE TableHist1
(
black VARCHAR(4),
red VARCHAR(4),
pink VARCHAR(4),
blue VARCHAR(4),
white VARCHAR(4)
);

CREATE TABLE TableHist2
(
black VARCHAR(4),
red VARCHAR(4),
pink VARCHAR(4),
blue VARCHAR(4),
white VARCHAR(4)
);