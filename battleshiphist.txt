--Colors table to identify actions/squares
--Black for ship
--gray for placing ship??
--red for hit
--pink for slected area to hit
--blue for water/miss
--white for blank area or nothing in it yet

CREATE TABLE TableHist
(
black VARCHAR(4),
red VARCHAR(4),
pink VARCHAR(4),
blue VARCHAR(4),
white VARCHAR(4)
);

